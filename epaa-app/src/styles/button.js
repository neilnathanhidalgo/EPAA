import { StyleSheet } from "react-native";

export default StyleSheet.create({
  button: {
    backgroundColor: "#FFA500",
    borderRadius: 5,
    alignItems: "center",
  },

  text: {
    color: "white",
    fontSize: 16,
    padding: 10,
  },
});
